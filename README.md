# JavaScript Counter

## 📌 Description
This project is a simple **interactive counter** developed using **vanilla JavaScript**.
It was created as a learning exercise to practice **DOM manipulation** and **event handling**, without using any JavaScript frameworks.

---

## 🎯 Project Goal
The goal of this project is to create a counter that:
- Displays an initial value of **0** when the page loads
- Allows the user to **increase** and **decrease** the value using buttons
- Updates the displayed value in real time

---

## 🛠 Technologies Used
- **HTML5** – Page structure
- **CSS / Bootstrap 5** – Styling and layout (visual purpose only)
- **Vanilla JavaScript** – Application logic and DOM manipulation

---

## ⚙️ Features
- Display of the current counter value
- **Increase (+)** button to increment the value
- **Decrease (−)** button to decrement the value
- **Reset button** to reset the counter value to 0
- Simple and responsive user interface
- Background with a **horizontal gradient from red to blue**

---
